server:
  port: 8380
  servlet:
    context-path: /spammer
spring:
  profiles:
    active: local
  application:
    name: spammer

spammer:
  message-interval-millis: 500
  worker-notification-url: http://localhost:8381/worker/notification